# Pinned for reproducible builds. Versions reflect the verified working set.
# When upgrading, bump here deliberately and re-run the test suite.
fastapi==0.115.11
uvicorn==0.34.0
sqlalchemy==2.0.38
pymssql==2.3.13
alembic==1.18.5
pydantic==2.10.6
pydantic-settings==2.14.2
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
python-multipart==0.0.32
slowapi==0.1.10
limits==5.8.0
# Google Drive client. Not present in the test venv (Drive is not exercised by
# the SQLite test suite); pinned to the known-compatible floor for google-auth
# 2.38.x. Verify/adjust against the deployment environment when provisioning.
google-api-python-client==2.120.0
google-auth==2.38.0
# Firebase Cloud Messaging (push notifications). Imported lazily — if it is not
# installed or no service-account file is configured, push is disabled and the
# app runs normally (notifications remain in-app).
firebase-admin==6.5.0
requests==2.32.3
cryptography==49.0.0
email-validator==2.3.0
tzdata==2025.1
python-docx==1.2.0
python-pptx==1.0.2

import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Deep Neon Tech Aesthetic)
  static const Color background = Color(0xFF0A0B10);
  static const Color surface = Color(0xFF131520);
  static const Color surfaceOverlay = Color(0x80131520);
  
  // Neon Accents
  static const Color primary = Color(0xFF00F2FE);    // Neon Teal / Cyan
  static const Color secondary = Color(0xFF8A2BE2);  // Electric Violet
  static const Color accent = Color(0xFFFF2A54);     // Hot Pink / Coral
  
  // Status Colors
  static const Color success = Color(0xFF00E676);    // Bright Green
  static const Color warning = Color(0xFFFFD600);    // Bright Amber
  static const Color error = Color(0xFFD50000);      // Crimson Red
  
  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9098B1);
  static const Color textMuted = Color(0xFF4A5060);
  
  // Border & Grid Colors
  static const Color border = Color(0x1AFFFFFF);      // Semi-transparent border
  static const Color borderGlow = Color(0x4D00F2FE);  // Glowing Cyan border
}
